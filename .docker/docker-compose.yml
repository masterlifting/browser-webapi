services:
  browser-api:
    container_name: browser-api
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    ports:
      - 8080:8080
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - USE_UI=false
      - BINARY_DATA_DIR=/usr/bin/chromium-browser
      - USER_DATA_DIR=/chrome_temp
      - IDLE_TIMEOUT_DAYS=7
