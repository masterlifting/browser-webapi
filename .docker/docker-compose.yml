services:
  browser-api:
    container_name: browser-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - LOG_LEVEL=info
      - USER_DATA_DIR=/tmp
    shm_size: 2gb
